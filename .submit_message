🧹 [Code Health] Replace 'any' type assertions with explicit specific union types in task item

🎯 **What:** The code health issue addressed was avoiding the use of the `any` type assertion when extracting the target value from the HTML select element in `src/components/tasks/task-item.tsx`.
💡 **Why:** Refactoring from `any` to `Task["priority"]` (which evaluates to `'low' | 'medium' | 'high' | 'urgent'`) improves maintainability by ensuring strong typing.
✅ **Verification:** I ran `npm run test` to confirm that the existing test suite passes, ran `npx tsc --noEmit` and ensured no behavior change.
✨ **Result:** A safer, strongly-typed event handler that correctly reflects the domain of the `Task` model's priority property.
